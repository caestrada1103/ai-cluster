-r requirements.txt
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
black==23.11.0
ruff==0.1.6
mypy==1.7.1
httpx==0.28.1
# mypy strict needs these — without them it errors with import-untyped on
# `import toml` / `import yaml` even with ignore_missing_imports=true.
types-toml
types-PyYAML
