# Coordinator runtime dependencies (see requirements-dev.txt for lint/test,
# scripts/requirements-scripts.txt for conversion/benchmark tools).
# M10: fastapi/starlette/pydantic-settings bumped off known-vulnerable
# versions (pip-audit against the previous pins: pydantic-settings 2.13.1 ->
# GHSA-4xgf-cpjx-pc3j; starlette 0.52.1 -> 5 distinct PYSEC advisories —
# PYSEC-2026-161/248/249/2280/2281). starlette is pinned explicitly (not
# just pulled in transitively) since fastapi's own dependency spec
# (`starlette>=0.46.0`) has no upper bound and would otherwise silently
# permit a downgrade back into the vulnerable range on a future `pip
# install -r` with a stale resolver cache.
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.41.0
grpcio==1.78.0
grpcio-tools==1.78.0
protobuf==6.33.5
pydantic==2.12.5
pydantic-settings==2.14.2
pyyaml==6.0.3
toml==0.10.2
prometheus-client==0.19.0

# Transparent HTTP/SSE proxy to worker-local llama-server instances
# (coordinator/proxy.py — Plan 13 agentic serving). Async client + streaming.
httpx==0.28.1

# Context compression middleware (coordinator/context_compression/) — see
# pending-work/12-context-compression-pipeline.md. Pure-Python-wheel installs,
# no build toolchain required.
tree-sitter==0.23.2
tree-sitter-python==0.23.6
tree-sitter-typescript==0.23.2
